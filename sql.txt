CREATE TABLE IF NOT EXISTS idcard_issued (
  identifier VARCHAR(64) NOT NULL PRIMARY KEY,
  issued_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS driverlicense_item_issued (
  identifier VARCHAR(64) NOT NULL PRIMARY KEY,
  issued_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);